[package]
name = "mint-sdk"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
admin-authority-core = { path = "../admin-authority-core" }
mint-core = { path = "../mint-core" }
mint-program = { path = "../mint-program" }
