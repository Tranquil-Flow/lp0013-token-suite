[package]
name = "admin_authority_spike-methods"
version = "0.1.0"
edition = "2021"

[build-dependencies]
risc0-build = "=3.0.5"

[dependencies]
risc0-zkvm = { version = "=3.0.5", features = ["std"] }
admin_authority_spike_core = { path = "../admin_authority_spike_core" }
