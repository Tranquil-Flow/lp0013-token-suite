# Solution: LP-0013 — Token program improvements: authorities

**Submitted by:** Tranquil-Flow

## Summary

This submission implements a mint-authority lifecycle for LEZ tokens: variable-supply minting by an authority, atomic authority rotation, permanent revocation to `None` for fixed-supply tokens, deterministic post-revocation rejection, SDK/CLI support, examples, and IDL artifacts.

The corrected LEZ guest is deployed and exercised on the public LEZ testnet (`https://testnet.lez.logos.co/`, real consensus, `RISC0_DEV_MODE=0`). The load-bearing 2026-06-27 lifecycle proves the reviewer-critical fix: two mints to the same holding account both confirm and accumulate (`60 + 40 = 100`), then a post-revocation mint to that already-existing holding is rejected by the authority guard rather than by an account-initialization side effect.

## Repository

Public repository:

```text
https://github.com/Tranquil-Flow/lp0013-token-suite
```

**License:** MIT OR Apache-2.0

## Public-testnet evidence


> **Current refresh status (2026-06-27):** reviewer `xAlisher` asked for a fresh redeploy/re-anchor because Logos reset `testnet.lez.logos.co` after this PR was submitted. We attempted that refresh. The corrected deploy transaction is currently included again as `793992258d88e69c63cbede6fabec3ff5768d84d824d7ee9f3170f85fb717dce`. The current public endpoint uses camelCase RPC. rc3 public lifecycle transactions fail with `InvalidSignature`; a newer LEZ wallet can get built-in `auth-transfer init` included (`63f03aeeac4e4ee6676b444b18a7415bf95e7331f671c4ac4a3221e12bf22fff`), but locally adapting LP-0013 signing to that newer public-message prehash path only produced submitted hashes that still did not land or mutate state (`3160a1d6af85f97cf29610beefb466c1f9f905f7c33bf79ae06feec22902da97`, `7c2086485a26e89af30da570a7276639c877eedda3587e1746a02feb4326dd15`). Therefore fresh current live lifecycle hashes are still required before the reviewer-requested re-anchor is complete; this submission does not claim the reset-era re-anchor is complete. See `docs/CURRENT_TESTNET_REFRESH.md`.

Network: `https://testnet.lez.logos.co/`

ProgramId / ImageID:

```text
338865e9549b18fb736020eaef87d5e20075b4250e10c00e08ea918c4871554a
```

Explorer-form base58 ProgramId:

```text
4UARaVcJJoLxebFAobocsZyzpJ5TTUvvhRtFuHtuHypd
```

Canonical lifecycle:

| Step | Transaction hash | Verdict |
|---|---|---|
| deploy_program | `793992258d88e69c63cbede6fabec3ff5768d84d824d7ee9f3170f85fb717dce` | `Some(ProgramDeployment)` |
| create_mint | `55908821088c98e898c4ef99e9a36e02856092f7afd0155f3457c25c5cf67746` | `Some(Public)` |
| create_holding | `8a37a8fb7200856c57d199ce081f2b744ed3cbaeec8326c83092f5ca05ac668f` | `Some(Public)` |
| mint_to(60) | `daf5aa91f35dff8250794c0dcfe932de473c651bd25c946d76f09a42cfdb6a97` | `Some(Public)` |
| mint_to(40) | `ed07b29c004a796d504814ddf1a9a0cfda373d1618398b620e330ccb529b3cce` | `Some(Public)` |
| set_mint_authority(None) | `719123f918df2aee42c4e69d36ba8860807b2a69c97a2927097d8313a508550e` | `Some(Public)` |
| mint_to(post-revoke) | `016043771c0cc60efaf158ec120a9bf341326967c881285878469503ddd3d4fa` | not included (`Transaction is None`) |

Final state read from chain:

- mint PDA `4gMBXeUskbUTzxoP8fJJEXj3jxTQz91m6ZW7fMsLMJq6`: `authority=None`, `supply=100`, `decimals=6`
- holding PDA `366n7Nj21EzD27BXRKE2hFDWPtJ1E2Fcx9RmqQoGRD7h`: `balance=100`
- signer / authority: `6HEYFUW4QbHPfdHTMPZLDeC6F5PL6suhSGJbTnsauhWJ`

Read-only re-verification:

```bash
bash scripts/demo-testnet-live.sh verify
```

Full requirements matrix: `docs/LP0013_REQUIREMENTS_MATRIX.md`.

If `wallet` is unavailable, build it from the LEZ v0.2.0 source tree as described in `docs/LEZ_PROOF_LOG.md` and `onchain-program/README.md`.

## What was fixed after the previous review

The rejected build used `#[account(init, pda)]` on the recipient holding account inside `mint_to`. That meant:

1. a second mint to the same holding failed because `init` rejected the already-claimed account; and
2. the post-revocation mint was rejected before the authority guard ran, so it did not prove revoked-authority enforcement.

The corrected guest splits holding creation from minting:

- `create_holding(#[account(init, pda)] holding, #[account(signer)] payer)` claims the holding once.
- `mint_to(#[account(mut, pda)] mint, #[account(mut, pda)] recipient_holding, #[account(signer)] authority, amount)` mutates an existing holding and performs `require_authority` before any state change.

The public-testnet lifecycle above demonstrates this distinction on chain: both mints land into the same holding, then the post-revocation mint targets the same existing holding and is rejected by the authority guard.

## Success-criteria map

| LP-0013 criterion | Status | Evidence |
|---|---|---|
| Variable-size tokens through minting authority | Met | `mint-core`, `mint-program`, `mint-sdk`, examples, and public-testnet two-mint lifecycle (`60 + 40 = 100`) |
| Mint authority set at token initialization | Met | `create_mint` instruction and testnet `create_mint` tx |
| Minting by the authority | Met | `mint_to(60)` and `mint_to(40)` txs confirmed on testnet |
| Authority rotation/revocation | Met | offline rotation tests and testnet `set_mint_authority(None)` tx |
| Fixed supply via revoked authority | Met | post-revocation mint not included; final mint supply remains `100` |
| At least two example integrations | Met | `examples/fixed-supply`, `examples/variable-supply`, plus `examples/config-pda-gated` |
| Self-sufficient library for RFP-001-style approval | Met | `admin-authority-core` and config-PDA-gated example |
| SDK/module for Logos-module interactions | Met | `mint-sdk`, `mint-cli`, and LEZ lifecycle client in `onchain-program/examples` |
| IDL using SPEL | Met | `idl/admin-authority.idl.spel-generated.json` and `idl/admin-authority.idl.spel-generated.rc3-testnet.json` are generated by `spel generate-idl`; hand-written IDL is only a caveated design reference |
| Atomic rotation/revocation | Met | failed operations clone/validate before commit; covered by unit tests |
| Deterministic revoked-authority rejection | Met | `AuthorityRevoked` / `require_authority` path; exact error string visible in localnet logs, state-level guard rejection proven on public testnet |
| CU cost documented | Partially met with platform caveat | `docs/BENCHMARKS.md`; public testnet does not expose per-tx CU, so local sequencer executor-cycle methodology is documented honestly |
| Deployed and tested on LEZ testnet | Met | ProgramId/ImageID and tx hashes above |
| End-to-end tests against LEZ sequencer in CI | Met via CI-included prepared-run path | `.github/workflows/ci.yml` includes hosted `local-sequencer-e2e-preflight` and manual self-hosted `local-sequencer-e2e`; `scripts/demo-localnet.sh` passed on a prepared M4 Pro LEZ/RISC0 host on 2026-06-09 with `RISC0_DEV_MODE=0` |
| Green CI on default branch | Met | implementation repo GitHub Actions; hosted Rust workspace and local-sequencer preflight passed after the standalone-localnet CI/preflight changes |
| README end-to-end usage | Met | `README.md`, `docs/SPEC_COMPLIANCE.md`, `onchain-program/README.md` |
| Reproducible demo script | Met | `scripts/demo-localnet.sh` for standalone local sequencer; `scripts/preflight-localnet-e2e.sh --report` for prerequisite reporting; `scripts/demo-testnet-live.sh verify` for public-testnet re-verification |
| Recorded narrated demo video | Met | fresh narrated public-testnet demo: https://youtu.be/rUgsCCPiQfo |

## Local verification commands

```bash
cargo fmt --all -- --check
cargo check --workspace
cargo test --workspace
cargo clippy --workspace --all-targets -- -D warnings
python3 scripts/validate-submission-docs.py
python3 -m pytest tests/test_validate_submission_docs.py -q
```

## Video status

Fresh narrated demo video: https://youtu.be/rUgsCCPiQfo. This recording is the final video evidence for the corrected public-testnet lifecycle.

## Honesty note

The 2026-06-03 public-testnet run (`ProgramId 4153e159…`, `ImageID 59e15341…`) is superseded historical evidence only. It used the pre-fix single-`init` holding pattern; the load-bearing fix evidence is the 2026-06-27 corrected run documented above.

## Terms & Conditions

By submitting this solution, I confirm that I have read and agree to the [Terms & Conditions](../TERMS.md), and that the implementation is original work published under MIT OR Apache-2.0 licensing.


### Current public-testnet refresh status (2026-06-26)

Reviewer `xAlisher` asked for a fresh redeploy/re-anchor because Logos reset `testnet.lez.logos.co` after the recorded June 4 lifecycle. We attempted the refresh on 2026-06-26. Read-only probing confirmed the current endpoint uses camelCase JSON-RPC (`checkHealth`, `getProgramIds`, `getAccountsNonces`, `getAccount`, `getTransaction`) and exposes `sendTransaction`. The corrected ProgramDeploymentTransaction is currently included again as `793992258d88e69c63cbede6fabec3ff5768d84d824d7ee9f3170f85fb717dce`, but the first public lifecycle transaction (`create_mint`, expected hash `3d8617cd607d179354894f2d6457a14c37cc0d0d95e028d9f8f47ad1e2ce6c37`) is rejected at sequencer precheck with `InvalidSignature`. Built-in wallet public transactions (`wallet auth-transfer init`) fail the same way, so this is tracked as a current public-testnet/wallet compatibility blocker rather than claimed as refreshed LP-0013 evidence. The guarded refresh helper is tracked as `scripts/lp0013-testnet-refresh-readiness.py`. Historical June 4 lifecycle hashes remain documented, and fresh current live lifecycle hashes are still required before claiming the reviewer-requested re-anchor is complete.
